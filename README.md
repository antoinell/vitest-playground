# Purpose of this repository

## demonstration of issue with substituted drive letters on Windows

[Issue with substituted drive letters on Windows](https://github.com/vitest-dev/vitest/issues/7170#issuecomment-2607531580)

## produce a javascript file containing a re-exported version of the `react-quiz-component` library

- when runnin `npm build` one obtains a file `dist/quiz-lib.js` which contains the reexported version of the library.


# genesis of the repository

```
npm create vite@latest my-react-app --template react

Select a framework: > React
Select a variant: > TypeScript + SWC (was thinking also of React Router V7)
```

